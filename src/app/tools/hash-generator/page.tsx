import HashGenerator from "@/components/tool/HashGenerator";

export const metadata = {
  title: "Free Online Hash Generator - MD5, SHA-1, SHA-256, SHA-512 | Exismic",
  description: "Compute MD5, SHA-1, SHA-256, and SHA-512 cryptographic hashes online instantly with real-time updates.",
};

export default function Page() {
  return <HashGenerator />;
}
